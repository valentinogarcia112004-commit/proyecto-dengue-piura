# Librerías utilizadas en el proyecto.
# Recomendación: reemplaza estas versiones por las exactas de tu entorno de Colab
# ejecutando en una celda: !pip freeze | grep -Ei "pandas|numpy|matplotlib|scikit-learn"
# y pegando el resultado aquí antes de tu entrega final.

pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
